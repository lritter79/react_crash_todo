//this is the entry point to react
//we're importing the library and the parent components
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//react dom is rendering the components into the div with the id 'root'
ReactDOM.render(<App />, document.getElementById('root'));
